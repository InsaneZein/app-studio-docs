export function applyReducerHash(reducerHash: any, initialState?: {}): (state: {} | undefined, action: any) => any;
export function dispatchActionsToStore(actions: any, store: any): {};
export class ReducerRegistry {
    constructor(initState?: {}, middlewares?: any[], composeEnhancersDefault?: typeof compose);
    store: any;
    reducers: {};
    getStore(): any;
    register(newReducers: any): () => void;
}
export const reduxRegistry: ReducerRegistry;
export default ReducerRegistry;
import { compose } from "redux";
